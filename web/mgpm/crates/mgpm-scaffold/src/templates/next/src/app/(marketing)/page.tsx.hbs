import Link from 'next/link';

export default function MarketingHome() {
  return (
    <div className="max-w-4xl mx-auto px-4 py-16">
      <section className="text-center mb-16">
        <h1 className="text-5xl font-bold mb-6">
          Welcome to {{name}}
        </h1>
        <p className="text-xl text-gray-600 mb-8 max-w-2xl mx-auto">
          A modern web application built with Next.js 15, TypeScript, and
          Tailwind CSS.
        </p>
        <div className="flex gap-4 justify-center">
          <Link
            href="/about"
            className="px-8 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-medium"
          >
            Learn More
          </Link>
          <Link
            href="/dashboard"
            className="px-8 py-3 border border-gray-300 rounded-lg hover:bg-gray-50 transition font-medium"
          >
            Dashboard
          </Link>
        </div>
      </section>

      <section className="grid md:grid-cols-3 gap-8">
        {[
          {
            title: 'Fast',
            desc: 'Server Components and streaming for instant page loads.',
          },
          {
            title: 'Type Safe',
            desc: 'Full TypeScript support with strict mode enabled.',
          },
          {
            title: 'Modern',
            desc: 'Tailwind CSS v4 with the latest React 19 features.',
          },
        ].map(({ title, desc }) => (
          <div key={title} className="p-6 border rounded-xl">
            <h3 className="text-lg font-semibold mb-2">{title}</h3>
            <p className="text-gray-600">{desc}</p>
          </div>
        ))}
      </section>
    </div>
  );
}
