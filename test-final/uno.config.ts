import { defineConfig } from "unocss";

export default defineConfig({
  rules: [],
  shortcuts: [],
  theme: {
    colors: {
      primary: {
        DEFAULT: "#3b82f6",
        foreground: "#ffffff",
      },
    },
  },
});