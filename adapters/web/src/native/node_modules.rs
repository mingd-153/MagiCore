/// Smart node_modules linker — hardlink-based, pnpm-style
use anyhow::Result;
use std::path::Path;

pub struct NodeModulesLinker;

impl NodeModulesLinker {
    pub fn new() -> Self {
        Self
    }

    pub fn link(&self, _store: &Path, _target: &Path, _packages: &[String]) -> Result<()> {
        Ok(())
    }
}
