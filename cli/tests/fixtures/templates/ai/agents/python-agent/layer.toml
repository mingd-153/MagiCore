[layer]
core = "ai"
category = "agents"
name = "python-agent"
description = "Python AI agent test template"
version = "0.1.0"

[files]
"pyproject.toml.hbs" = { render = "pyproject.toml" }
"requirements.lock" = { copy = true }
"README.md" = { copy = true }
