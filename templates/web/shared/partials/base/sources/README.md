# {{project_name}}

Generated by MegaGate `web` core.

- Mode: `{{mode}}`
- Frameworks: `{{frameworks}}`
- Template: `{{template}}`

## Notes

- This scaffold is managed by MegaGate templates under `templates/web/`.
- `mg.lock` is the MegaGate-native lockfile for resolved web dependencies.
- `.megagate/web.toml` stores project template metadata for the web core.

## Next

- Run `mg install-web` in this directory to install web dependencies with MegaGate.
