name = "{{project_name}}"
version = "0.1.0"
ecosystem = "web"
mode = "{{mode}}"
frameworks = [{{frameworks}}]
template = "{{template}}"
features = [{{features}}]

[execution]
architecture = "{{execution_architecture}}"
lane = "{{execution_lane}}"
compatibility_layer = "{{execution_compatibility_layer}}"
native_targets = [{{execution_native_targets}}]
