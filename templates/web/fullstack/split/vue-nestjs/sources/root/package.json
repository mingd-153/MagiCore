{
  "name": "{{project_slug}}",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "mg --core web dev",
    "dev:client": "vite",
    "dev:server": "cd server && mg --core web dev",
    "build": "vite build",
    "preview": "vite preview"
  }
}
