# Rust
target/

# Test scaffold artifacts
/test-fw/
/cli/test-fastapi/

# Bazel
bazel-*/
bazel-out/
bazel-testlogs/
bazel-bin/
bazel-genfiles/
.bazelrc.local

# Nix
result
result-*

# TypeScript/Node
node_modules/
dist/
*.tsbuildinfo
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-lock.yaml

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Task docs (internal, not public)
task/

# Secrets
*.key
*.pem
.env
.env.local
deploy/docker/registry.env
deploy/docker/data/

# Logs
logs/
*.log

# Build artifacts
*.tgz
*.tar.gz
!cli/embedded/*.tar.gz

# Coverage
coverage/
*.lcov

# Temporary
tmp/
temp/
*.tmp
*.cache

# Zig cache
.zig-cache/

# Debug symbols
*.dSYM/

# Benchmark artifacts
benchmark_brutal_results_*.md
benchmark_brutal_results_*.json
benchmark_brutal_results_*.status.tsv
magicore-bench-*
mgc-brutal-*
mgc-test-*
mgc-bench-*
# Measured benchmark raw data + summaries (local-only evidence, not source).
# Dữ liệu đo benchmark raw + tổng hợp — bằng chứng local-only, không phải source.
benchmark/results/
# Measured benchmark outputs (bench_v2/aggregate_v2/export_bench runs) —
# local-only measurement evidence, never commit generated numbers.
# Output đo benchmark (run của bench_v2/aggregate_v2/export_bench) — bằng
# chứng đo local-only, không bao giờ commit số sinh ra.
benchmark/results/v2/
benchmark/results/p1_suite/
benchmark/results/*_run*.json
benchmark/results/*_run*.log
benchmark/results/*_analysis*.json
benchmark/results/SUMMARY_PROVISIONAL*
benchmark/results/Raw_ALL_RUNS*
benchmark/results/RAW_ALL_RUNS*
benchmark/results/*_SUMMARY*.txt
benchmark/*_measure*.json
benchmark/*_measure*.md
benchmark/*.bench.json
benchmark/*.bench.md
# Rust criterion/export_bench captured outputs (cargo bench output dumps).
# Output capture của criterion/export_bench (kết xuất cargo bench).

# ── Internal documentation (local-only, NOT for git) ─────────────────────────

# Internal evaluation docs & audit reports
*AUDIT*.md
*REPORT*.md
*SUMMARY*.md
*REVIEW*.md
*Checklist*.md
*ProgressReport*.md
report-v2-changes.md
ARCHITECTURE_AUDIT.md
SECURITY_AUDIT.md
MAGICORE_P2_REPORT.md
OPTIMIZATION_SUMMARY.md
CODEBASE_REVIEW.md
REPORT.md

# System design docs (working drafts — read internally, never push)
sys-mgc/

# Internal specs, guides and notes
docs/*
docs/internal-reports/  # Week reports, audit reports (nội bộ)
docs/guides/
scripts/notes/

# Per-component internal docs
cli/docs/
adapters/*/docs/
core/crates/*/docs/
tools/*/docs/

# Allow all public-facing documentation
!**/README.md
!README.md
!CHANGELOG.md
!CONTRIBUTING.md
packaging/artifacts/
