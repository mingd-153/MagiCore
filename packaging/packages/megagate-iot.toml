name = "megagate-iot"
binary = "mg"
description = "MegaGate single-core IoT distribution"
mode = "single-core"
primary_core = "iot"
no_default_features = true
features = ["iot"]
install_hint = "brew install megagate-iot"
