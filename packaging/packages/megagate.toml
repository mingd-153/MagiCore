name = "megagate"
binary = "mg"
description = "MegaGate full multi-core distribution"
mode = "multi-core"
primary_core = "all"
no_default_features = true
features = ["all"]
install_hint = "brew install megagate"
