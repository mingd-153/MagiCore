name = "megagate-web"
binary = "mg"
description = "MegaGate single-core web distribution"
mode = "single-core"
primary_core = "web"
no_default_features = true
features = ["web"]
install_hint = "brew install megagate-web  |  curl -fsSL https://raw.githubusercontent.com/mingd-153/MegaGate/main/scripts/install-from-gh.sh | bash -s -- --package megagate-web"
