name = "magicore-app"
binary = "mgc"
description = "MagiCore single-core app distribution"
mode = "single-core"
primary_core = "app"
no_default_features = true
features = ["app"]
install_hint = "curl -fsSL https://raw.githubusercontent.com/mingd-153/MagiCore/main/scripts/install-from-gh.sh | bash -s -- --package magicore-app"
