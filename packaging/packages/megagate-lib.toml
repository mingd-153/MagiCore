name = "megagate-lib"
binary = "mg"
description = "MegaGate single-core library distribution"
mode = "single-core"
primary_core = "lib"
no_default_features = true
features = ["lib"]
install_hint = "curl -fsSL https://raw.githubusercontent.com/mingd-153/MegaGate/main/scripts/install-from-gh.sh | bash -s -- --package megagate-lib"
