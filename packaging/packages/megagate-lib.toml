name = "megagate-lib"
binary = "mg"
description = "MegaGate single-core library distribution"
mode = "single-core"
primary_core = "lib"
no_default_features = true
features = ["lib"]
install_hint = "brew install megagate-lib"
