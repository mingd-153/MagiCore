name = "megagate-game"
binary = "mg"
description = "MegaGate single-core game distribution"
mode = "single-core"
primary_core = "game"
no_default_features = true
features = ["game"]
install_hint = "brew install megagate-game"
