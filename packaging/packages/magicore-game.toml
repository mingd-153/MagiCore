name = "magicore-game"
binary = "mgc"
description = "MagiCore single-core game distribution"
mode = "single-core"
primary_core = "game"
no_default_features = true
features = ["game"]
install_hint = "curl -fsSL https://raw.githubusercontent.com/mingd-153/MagiCore/main/scripts/install-from-gh.sh | bash -s -- --package magicore-game"
