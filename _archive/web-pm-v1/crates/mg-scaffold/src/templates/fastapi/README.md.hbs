# {{name}}

FastAPI REST API built with Python 3.12.

## Commands

```bash
mgpm dev       # uvicorn app.main:app --reload
mgpm build     # Already interpreted, no build needed
mgpm start     # uvicorn app.main:app
```
