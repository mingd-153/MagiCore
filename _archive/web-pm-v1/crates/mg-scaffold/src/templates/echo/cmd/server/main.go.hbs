package main

import (
	"log"
	"os"
	"{{name}}/internal/router"
)

func main() {
	port := os.Getenv("PORT")
	if port == "" {
		port = "8080"
	}

	e := router.Setup()

	log.Printf("Server running on http://localhost:%s", port)
	if err := e.Start(":" + port); err != nil {
		log.Fatal(err)
	}
}
