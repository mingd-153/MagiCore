import { createFileRoute, Link } from '@tanstack/react-router';

export const Route = createFileRoute('/')({
  component: Home,
});

function Home() {
  return (
    <div className="home">
      <h1>Welcome to {{name}}</h1>
      <p>Built with TanStack Start + React</p>
      <Link to="/about">Learn more</Link>
    </div>
  );
}
