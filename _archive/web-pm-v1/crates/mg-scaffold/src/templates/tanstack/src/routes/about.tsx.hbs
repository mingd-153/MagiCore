import { createFileRoute, Link } from '@tanstack/react-router';

export const Route = createFileRoute('/about')({
  component: About,
});

function About() {
  return (
    <div>
      <h1>About {{name}}</h1>
      <p>A modern web app built with TanStack Start.</p>
      <Link to="/">Go home</Link>
    </div>
  );
}
