defmodule {{pascalCase name}}.MixProject do
  use Mix.Project

  def project do
    [
      app: :{{name}},
      version: "{{version}}",
      elixir: "~> 1.17",
      start_permanent: Mix.env() == :prod,
      deps: deps()
    ]
  end

  def application do
    [extra_applications: [:logger], mod: {.{{pascalCase name}}.Application, []}]
  end

  defp deps do
    [
      {:phoenix, "~> 1.7"},
      {:phoenix_ecto, "~> 4.5"},
      {:ecto_sql, "~> 3.11"},
      {:postgrex, ">= 0.0.0"},
      {:phoenix_live_dashboard, "~> 0.8"},
      {:jason, "~> 1.4"},
      {:corsica, "~> 2.0"}
    ]
  end
end
